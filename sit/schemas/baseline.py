"""Baseline schema variant: precise semantic indices.

TCA-quality Pydantic models with domain-accurate field names,
rich descriptions, and expressive enum members. This is the
"correct semantic content" condition — the schema as a
practitioner would write it.
"""
