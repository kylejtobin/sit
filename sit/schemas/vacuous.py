"""Vacuous schema variant: absent semantic indices.

Structurally isomorphic to baseline. Field names are structural
identifiers (field_1, field_2). Enum members are positional
(OPTION_A, OPTION_B). Descriptions are empty or generic.
Same types, same cardinality, same nesting — zero semantic content.
"""
