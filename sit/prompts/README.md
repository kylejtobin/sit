# Prompts

50 customer profiles derived from the public Telco Customer Churn dataset
(`scikit-learn/churn-prediction` on Hugging Face).

Each profile is a plain text paragraph with hand-labeled ground truth
for each enum field. Stored as `profiles.json`.
